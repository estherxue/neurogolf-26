"""rb018 — import alias so `family_test.py rb018` resolves the module.

The implementation lives in family_rb018.py (per the requested file path).
"""
from family_rb018 import candidates, build_18, _ref  # noqa: F401
